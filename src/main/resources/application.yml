spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bd_spring_security_docker
    username: admin
    password: magadiflo
  jpa:
    database-platform: org.springframework.orm.jpa.vendor.Database.MYSQL
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        globally_quoted_identifiers: true
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true